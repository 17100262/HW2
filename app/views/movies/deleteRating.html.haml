
%h1 Delete Movies by rating
= form_tag delete_by_rating_path,:id=>"ratings_form", :method => :get do
  Include:
  - @all_ratings.each do |rating|
    = rating
    = check_box_tag "ratings[#{rating}]","1", @ratings.include?(rating)
  = submit_tag 'Refresh list',name:"ratings_submit"

%table#movies
  %thead
    %tr
      %th= link_to "Movie Title",{id:"title_header"}
      %th Rating
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title
        %td= movie.rating
        %td= button_to 'Delete', movie_path(movie), :method => :delete, :confirm => 'Are you sure?'
// = button_to 'Delete All', movie_path(@movies), :method => :delete_all, :confirm => 'Are you sure?'